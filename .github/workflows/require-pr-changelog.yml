# 兼容入口：逻辑在 .github/actions/require-pr-changelog
# 业务仓请用本地 job + composite action，Ruleset 只勾「PR Changelog」

name: Require PR Changelog section

on:
  workflow_call:

jobs:
  check:
    name: PR Changelog
    runs-on: ubuntu-latest
    steps:
      - uses: iot-daci/template/.github/actions/require-pr-changelog@main
