name: publish npm package

on:
  workflow_call:
    inputs:
      workdir:
        type: string
        required: false
        default: "."
      package_path:
        type: string
        required: true
        description: "Package directory relative to workdir, e.g. packages/admin-core"
      publish_command:
        type: string
        required: false
        default: ""
        description: "Publish command relative to workdir; default pnpm -C <package_path> publish --no-git-checks"
      skip_if_exists:
        type: boolean
        required: false
        default: true
        description: "Skip publish when the same name@version already exists in registry"
      registry_url:
        type: string
        required: false
        default: "https://packages.aliyun.com/637eea4f126bcb821717c7ff/npm/npm-registry/"
    secrets:
      NPM_AUTH_TOKEN:
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    container:
      image: lzk90s/js-develop-env:alpine
      volumes:
        - /root/.m2:/root/.m2
    defaults:
      run:
        working-directory: ${{ inputs.workdir }}
    env:
      NPM_CONFIG_USERCONFIG: /root/.npmrc
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Init npm env
        run: init_env.sh

      - name: Configure npm registry auth
        env:
          NPM_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
        run: |
          REGISTRY="${{ inputs.registry_url }}"
          REGISTRY_HOST="${REGISTRY#https://}"
          REGISTRY_HOST="${REGISTRY_HOST%/}"
          printf '%s\n' \
            "registry=${REGISTRY}" \
            "always-auth=true" \
            "//${REGISTRY_HOST}/:_authToken=${NPM_AUTH_TOKEN}" \
            "cache=/root/.npm" \
            > /root/.npmrc
          echo "Configured registry=${REGISTRY}"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Publish package
        env:
          NPM_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
        run: |
          PKG=$(node -p "require('./${{ inputs.package_path }}/package.json').name")
          VERSION=$(node -p "require('./${{ inputs.package_path }}/package.json').version")
          echo "package=${PKG} version=${VERSION}"

          if [ "${{ inputs.skip_if_exists }}" = "true" ]; then
            if npm view "${PKG}@${VERSION}" version >/dev/null 2>&1; then
              echo "${PKG}@${VERSION} already published, skip"
              exit 0
            fi
          fi

          if [ -n "${{ inputs.publish_command }}" ]; then
            ${{ inputs.publish_command }}
          else
            pnpm -C "${{ inputs.package_path }}" publish --no-git-checks
          fi
